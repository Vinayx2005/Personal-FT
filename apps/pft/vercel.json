{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "ignoreCommand": "git diff --quiet ${VERCEL_GIT_PREVIOUS_SHA:-HEAD^} HEAD -- ./",
  "crons": [
    {
      "path": "/api/cron/sip-debit",
      "schedule": "0 3 * * *"
    }
  ]
}
